import ScenariosPage from "./ScenariosPage";

/**
 * A project's landing view is its scenarios list (with the project summary header).
 */
export default function ProjectDetailPage() {
  return <ScenariosPage />;
}
